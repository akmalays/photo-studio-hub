{
  "name": "photo-studio-hub",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "frontend"
  ],
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "scripts": {
    "dev": "bun run --cwd frontend dev",
    "build": "bun run --cwd frontend build",
    "preview": "bun run --cwd frontend preview",
    "install:all": "bun install",
    "backup": "bun run supabase/backup.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "@supabase/supabase-js": "^2.101.1",
    "@types/node": "^22.14.0",
    "concurrently": "^8.2.2",
    "typescript": "^5.8.3"
  }
}
